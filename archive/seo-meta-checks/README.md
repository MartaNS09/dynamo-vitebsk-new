Saved HTML snapshots from manual SEO/meta checks (curl exports).

- `meta_check.html` — blog article page snapshot
- `meta_check2.html` — second check snapshot

These are reference artifacts only, not used by the application build.
